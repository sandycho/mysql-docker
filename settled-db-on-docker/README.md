Paste your dump.sql file (exported database file) into ./database folder.

In the root path (same level that this file) execute: 
    `docker build .`

Check docker documentation to modify, e.i, container name, etc.